class DeployLocationActor extends Actor placeable;

// Used as a matinee base for X2Action_DynamicDeployment.
// Codebase has PointInSpace for this purpose, but it comes with some strings attached that may or may not cause issues, so using my own.

defaultproperties
{
    bMovable = true
}
